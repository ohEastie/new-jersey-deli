<section class="hero" id="top">
	<div class="hero__media" aria-hidden="true">
		<img class="hero__slide hero__slide--one" src="/images/FEATUREDSANDchivito.avif" alt="" loading="eager" fetchpriority="high" />
		<img class="hero__slide hero__slide--two" src="/images/BREAKFASTperico.avif" alt="" loading="eager" />
		<img class="hero__slide hero__slide--three" src="/images/FEATUREDSANDcuban.avif" alt="" loading="eager" />
	</div>
	<div class="hero__overlay"></div>

	<div class="hero__content container">
		<div class="hero__copy">
			<p class="eyebrow hero__eyebrow" data-i18n="hero.eyebrow">Family-Owned Since 1994</p>
			<h1 data-i18n="hero.title">New Jersey Grocery & Deli</h1>
			<p class="hero__lead" data-i18n="hero.lead">
				A South Florida neighborhood deli rooted in Uruguayan family values, fresh
				sandwiches, everyday groceries, and service that remembers your name.
			</p>
			<div class="hero__actions">
				<a class="btn btn--primary" data-i18n="hero.ctaOrder" href="#order">Order online</a>
				<a class="btn btn--secondary" data-i18n="hero.ctaLocations" href="#locations">Find a location</a>
			</div>
		</div>

		<div class="hero__panel" aria-label="Business highlights">
			<div>
				<strong>1994</strong>
				<span data-i18n="hero.statFounded">Founded by Walter & Gloria</span>
			</div>
			<div>
				<strong data-i18n="hero.statLocationsNumber">2 Locations</strong>
				<span data-i18n="hero.statLocations">Serving Oakland Park & Margate</span>
			</div>
			<div>
				<strong>2017</strong>
				<span data-i18n="hero.statMargate">Margate Expansion</span>
			</div>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		display: grid;
		min-height: 78svh;
		overflow: hidden;
		color: var(--white);
	}

	.hero__media,
	.hero__overlay,
	.hero__content {
		grid-area: 1 / 1;
	}

	.hero__media {
		position: relative;
		overflow: hidden;
	}

	.hero__slide {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: 54% center;
		opacity: 0;
		transform: scale(1.005);
		animation: hero-fade 18s infinite;
	}

	.hero__slide--two {
		animation-delay: 6s;
		object-position: 54% center;
	}

	.hero__slide--three {
		animation-delay: 12s;
		object-position: 52% center;
	}

	@keyframes hero-fade {
		0%,
		28% {
			opacity: 1;
		}

		36%,
		92% {
			opacity: 0;
		}

		100% {
			opacity: 1;
		}
	}

	.hero__overlay {
		background:
			linear-gradient(90deg, rgba(3, 28, 52, 0.95), rgba(7, 55, 99, 0.74) 44%, rgba(7, 55, 99, 0.22)),
			linear-gradient(0deg, rgba(23, 32, 42, 0.58), rgba(23, 32, 42, 0.06));
	}

	.hero__content {
		position: relative;
		display: grid;
		align-content: center;
		gap: 2rem;
		min-height: 78svh;
		padding: 4.25rem 1rem 4.75rem;
	}

	.hero__copy {
		max-width: 720px;
	}

	.hero__eyebrow {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		margin-bottom: 1rem;
		padding: 0.42rem 0.72rem;
		border: 1px solid rgba(245, 184, 46, 0.38);
		border-radius: 999px;
		background: rgba(7, 55, 99, 0.38);
		color: var(--gold);
		font-size: 0.72rem;
		letter-spacing: 0.18em;
		text-shadow: 0 2px 18px rgba(0, 0, 0, 0.22);
	}

	.hero h1 {
		max-width: 12ch;
		margin-bottom: 1.1rem;
		font-size: clamp(2.65rem, 10.4vw, 5.7rem);
		text-shadow: 0 5px 36px rgba(0, 0, 0, 0.36);
	}

	.hero__lead {
		max-width: 680px;
		margin-bottom: 0;
		color: rgba(255, 253, 248, 0.94);
		font-size: clamp(1.14rem, 3.8vw, 1.42rem);
		font-weight: 550;
		line-height: 1.55;
		text-shadow: 0 2px 16px rgba(0, 0, 0, 0.24);
	}

	.hero__actions {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		width: min(100%, 430px);
		margin-top: 1.8rem;
	}

	.hero__actions .btn {
		min-height: 3.35rem;
		padding-inline: 1.35rem;
	}

	.hero__panel {
		display: grid;
		gap: 0.72rem;
		width: min(100%, 560px);
		padding: 0.72rem;
		border: 1px solid rgba(255, 253, 248, 0.2);
		border-radius: var(--radius);
		background: rgba(3, 28, 52, 0.88);
		box-shadow: 0 18px 45px rgba(0, 0, 0, 0.18);
	}

	.hero__panel div {
		min-height: 108px;
		padding: 0.92rem;
		border-radius: 6px;
		background: rgba(7, 55, 99, 0.86);
		border: 1px solid rgba(255, 253, 248, 0.16);
	}

	.hero__panel strong,
	.hero__panel span {
		display: block;
	}

	.hero__panel strong {
		color: var(--gold);
		font-family: Georgia, "Times New Roman", serif;
		font-size: clamp(1.45rem, 5vw, 2rem);
		line-height: 1.05;
		text-shadow: none;
	}

	.hero__panel span {
		max-width: 16ch;
		margin-top: 0.65rem;
		color: var(--white);
		font-size: 0.92rem;
		font-weight: 850;
		line-height: 1.32;
		text-shadow: none;
	}

	@media (min-width: 680px) {
		.hero__actions {
			flex-direction: row;
			width: auto;
		}

		.hero__content {
			padding-inline: 1.5rem;
		}

		.hero__panel {
			grid-template-columns: repeat(3, minmax(0, 1fr));
		}
	}

	@media (min-width: 1024px) {
		.hero__content {
			grid-template-columns: minmax(0, 1fr) 360px;
			align-items: center;
			gap: 2.5rem;
		}

		.hero__panel {
			grid-template-columns: 1fr;
			justify-self: end;
			margin-top: 3.2rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.hero__slide {
			animation: none;
			opacity: 0;
		}

		.hero__slide--one {
			opacity: 1;
		}
	}
</style>
