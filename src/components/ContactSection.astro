<section class="section section--white contact" id="contact">
	<div class="container contact__grid">
		<div>
			<p class="eyebrow" data-i18n="contact.eyebrow">Contact</p>
			<h2 data-i18n="contact.title">Call your neighborhood deli.</h2>
			<p class="lead" data-i18n="contact.copy">
				Questions about today's specials, groceries, or ordering ahead? Reach the location
				closest to you and we will be happy to help.
			</p>
			<a class="btn btn--primary contact__order" data-i18n="contact.order" href="#order">Order online</a>
		</div>

		<div class="contact-panel">
			<a class="contact-line" href="tel:+19545619211">
				<span data-i18n="contact.oakland">Oakland Park</span>
				<strong>(954) 561-9211</strong>
				<small>192 E Prospect Rd</small>
			</a>
			<a class="contact-line" href="tel:+17542226760">
				<span data-i18n="contact.margate">Margate</span>
				<strong>(754) 222-6760</strong>
				<small>5416 W Atlantic Blvd</small>
			</a>
			<div class="contact-panel__footer">
				<span data-i18n="contact.footer">Proudly family-owned and serving South Florida since 1994.</span>
			</div>
		</div>
	</div>
</section>

<footer class="footer">
	<div class="container footer__grid">
		<div class="footer__brand">
			<img src="/images/logo.png" alt="" width="84" height="84" />
			<div>
				<strong>New Jersey Grocery & Deli</strong>
				<span data-i18n="footer.copy">Family-Owned Since 1994</span>
			</div>
		</div>

		<div class="footer__columns">
			<div>
				<h2 data-i18n="footer.locations">Locations</h2>
				<a href="#locations" data-i18n="contact.oakland">Oakland Park</a>
				<a href="#locations" data-i18n="contact.margate">Margate</a>
			</div>
			<div>
				<h2 data-i18n="footer.order">Order Online</h2>
				<a href="https://order.online/store/303113?pickup=true&redirected=true" target="_blank" rel="noreferrer">DoorDash</a>
				<a href="https://www.order.store/store/new-jersey-deli-2/RdKfeHo8V3C5XNzBEly_wg?from_challenge=1" target="_blank" rel="noreferrer">Uber Eats</a>
			</div>
			<div>
				<h2 data-i18n="footer.hours">Hours</h2>
				<span>Mon-Fri 7:00 AM - 4:00 PM</span>
				<span>Sat-Sun 8:00 AM - 4:00 PM</span>
			</div>
			<div>
				<h2 data-i18n="footer.call">Phone</h2>
				<a href="tel:+19545619211">(954) 561-9211</a>
				<a href="tel:+17542226760">(754) 222-6760</a>
			</div>
		</div>
	</div>
</footer>

<a class="floating-order" data-i18n="floating.order" href="#order">Order Online</a>

<style>
	.contact__grid {
		display: grid;
		gap: 2rem;
		align-items: center;
	}

	.contact__order {
		margin-top: 0.4rem;
	}

	.contact-panel {
		display: grid;
		gap: 0.85rem;
		padding: 1rem;
		border-radius: var(--radius);
		background:
			linear-gradient(135deg, rgba(245, 184, 46, 0.12), rgba(255, 253, 248, 0.02)),
			var(--blue-deep);
		color: var(--white);
		box-shadow: var(--shadow);
	}

	.contact-line {
		display: grid;
		gap: 0.2rem;
		padding: 1rem;
		border: 1px solid rgba(255, 253, 248, 0.18);
		border-radius: var(--radius);
		background: rgba(255, 253, 248, 0.07);
		transition:
			transform 180ms ease,
			background 180ms ease;
	}

	.contact-line:hover {
		transform: translateY(-2px);
		background: rgba(255, 253, 248, 0.12);
	}

	.contact-line span {
		color: var(--gold);
		font-size: 0.8rem;
		font-weight: 900;
		text-transform: uppercase;
	}

	.contact-line strong {
		font-size: 1.55rem;
	}

	.contact-line small,
	.contact-panel__footer {
		color: rgba(255, 253, 248, 0.76);
	}

	.contact-line small {
		font-size: 0.9rem;
		font-weight: 700;
	}

	.contact-panel__footer {
		padding: 0.35rem 0.2rem 0;
		font-size: 0.95rem;
	}

	.footer {
		padding: 2.5rem 1rem 6rem;
		background: var(--blue-deep);
		color: var(--white);
	}

	.footer__grid,
	.footer__brand,
	.footer__columns,
	.footer__columns div {
		display: grid;
	}

	.footer__grid {
		gap: 2rem;
	}

	.footer__brand {
		grid-template-columns: auto minmax(0, 1fr);
		gap: 0.9rem;
		align-items: center;
	}

	.footer__brand img {
		width: 4.8rem;
		height: auto;
		border-radius: 999px;
	}

	.footer__brand strong,
	.footer__brand span {
		display: block;
	}

	.footer__brand strong {
		font-family: Georgia, "Times New Roman", serif;
		font-size: 1.5rem;
		line-height: 1.08;
	}

	.footer__brand span {
		margin-top: 0.3rem;
		color: var(--gold);
		font-weight: 900;
		text-transform: uppercase;
	}

	.footer__columns {
		gap: 1.4rem;
	}

	.footer__columns div {
		gap: 0.32rem;
		align-content: start;
	}

	.footer__columns h2 {
		margin-bottom: 0.35rem;
		color: var(--gold);
		font-family: inherit;
		font-size: 0.8rem;
		font-weight: 950;
		text-transform: uppercase;
	}

	.footer__columns a,
	.footer__columns span {
		color: rgba(255, 253, 248, 0.78);
		font-size: 0.95rem;
		font-weight: 750;
	}

	.footer__columns a:hover {
		color: var(--white);
	}

	.floating-order {
		position: fixed;
		right: 1rem;
		bottom: 1rem;
		z-index: 30;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 3rem;
		padding: 0.8rem 1rem;
		border-radius: var(--radius);
		background: var(--gold);
		color: var(--blue-deep);
		box-shadow: 0 16px 42px rgba(7, 55, 99, 0.28);
		font-weight: 950;
		text-transform: uppercase;
	}

	@media (min-width: 760px) {
		.contact__grid {
			grid-template-columns: minmax(0, 1fr) minmax(340px, 0.54fr);
			gap: 3rem;
		}

		.footer {
			padding-bottom: 2.5rem;
		}

		.footer__columns {
			grid-template-columns: repeat(4, minmax(0, 1fr));
		}

		.floating-order {
			display: none;
		}
	}

	@media (min-width: 980px) {
		.footer__grid {
			grid-template-columns: minmax(260px, 0.42fr) minmax(0, 1fr);
			align-items: start;
		}
	}
</style>
