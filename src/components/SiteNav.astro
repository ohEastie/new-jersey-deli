<header class="site-header">
	<nav class="nav container" aria-label="Primary navigation">
		<a class="brand" href="#top" aria-label="New Jersey Grocery & Deli home">
			<img class="brand-mark" src="/images/logo.png" alt="" width="56" height="56" />
			<span>
				<strong>New Jersey</strong>
				<small>Grocery & Deli</small>
			</span>
		</a>

		<button class="menu-toggle" type="button" aria-expanded="false" aria-controls="site-menu">
			<span class="menu-toggle__bar"></span>
			<span class="menu-toggle__bar"></span>
			<span class="menu-toggle__bar"></span>
			<span class="sr-only">Menu</span>
		</button>

		<div class="nav-panel" id="site-menu">
			<a data-i18n="nav.story" href="#story">Story</a>
			<a data-i18n="nav.locations" href="#locations">Locations</a>
			<a data-i18n="nav.hours" href="#hours">Hours</a>
			<a data-i18n="nav.products" href="#products">Products</a>
			<a data-i18n="nav.contact" href="#contact">Contact</a>

			<div class="language-toggle" aria-label="Language selector">
				<button class="language-toggle__option is-active" type="button" data-lang-option="en" aria-pressed="true">EN</button>
				<button class="language-toggle__option" type="button" data-lang-option="es" aria-pressed="false">ES</button>
			</div>
		</div>
	</nav>
</header>

<style>
	.site-header {
		position: sticky;
		top: 0;
		z-index: 20;
		background: rgba(255, 253, 248, 0.9);
		border-bottom: 1px solid rgba(7, 55, 99, 0.1);
		backdrop-filter: blur(16px);
	}

	.nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		min-height: 4.75rem;
		gap: 1rem;
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		min-width: 0;
	}

	.brand-mark {
		width: 2.8rem;
		aspect-ratio: 1;
		border-radius: 999px;
		object-fit: cover;
		object-position: center;
		box-shadow: 0 10px 24px rgba(0, 91, 187, 0.16);
	}

	.brand strong,
	.brand small {
		display: block;
		white-space: nowrap;
	}

	.brand strong {
		font-size: 1rem;
		line-height: 1.05;
	}

	.brand small {
		color: var(--ink-soft);
		font-size: 0.78rem;
		font-weight: 800;
		text-transform: uppercase;
	}

	.menu-toggle {
		display: inline-grid;
		place-items: center;
		width: 2.85rem;
		aspect-ratio: 1;
		border: 1px solid rgba(7, 55, 99, 0.16);
		border-radius: var(--radius);
		background: var(--white);
		cursor: pointer;
	}

	.menu-toggle__bar {
		width: 1.2rem;
		height: 2px;
		margin: 2px 0;
		background: var(--blue-deep);
	}

	.nav-panel {
		position: fixed;
		inset: 4.75rem 1rem auto;
		display: none;
		flex-direction: column;
		gap: 0.35rem;
		padding: 1rem;
		border: 1px solid rgba(7, 55, 99, 0.12);
		border-radius: var(--radius);
		background: var(--white);
		box-shadow: var(--shadow);
	}

	.nav-panel.is-open {
		display: flex;
	}

	.nav-panel a {
		padding: 0.78rem;
		border-radius: var(--radius);
		font-weight: 800;
		color: var(--blue-deep);
		transition:
			background 180ms ease,
			color 180ms ease;
	}

	.nav-panel a:hover {
		background: var(--blue-soft);
		color: var(--blue);
	}

	.language-toggle {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 0.2rem;
		margin-top: 0.5rem;
		padding: 0.25rem;
		border: 1px solid rgba(0, 91, 187, 0.16);
		border-radius: var(--radius);
		background: var(--blue-soft);
	}

	.language-toggle__option {
		border: 0;
		border-radius: 6px;
		background: transparent;
		color: var(--blue-deep);
		cursor: pointer;
		font-size: 0.85rem;
		font-weight: 900;
	}

	.language-toggle__option.is-active {
		background: var(--blue);
		color: var(--white);
		box-shadow: 0 8px 20px rgba(0, 91, 187, 0.18);
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	@media (min-width: 880px) {
		.menu-toggle {
			display: none;
		}

		.nav-panel {
			position: static;
			display: flex;
			flex-direction: row;
			align-items: center;
			gap: 0.2rem;
			padding: 0;
			border: 0;
			background: transparent;
			box-shadow: none;
		}

		.nav-panel a {
			padding: 0.65rem 0.78rem;
			font-size: 0.95rem;
		}

		.language-toggle {
			width: 5.75rem;
			margin: 0 0 0 0.6rem;
			background: var(--white);
		}
	}
</style>
