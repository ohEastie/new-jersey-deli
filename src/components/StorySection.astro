<section class="section section--cream story" id="story">
	<div class="container story__grid">
		<div class="story__copy">
			<p class="eyebrow" data-i18n="story.eyebrow">Our story</p>
			<h2 data-i18n="story.title">A Uruguayan family story, served daily in South Florida.</h2>
			<p class="lead" data-i18n="story.copy">
				In 1994, Walter and Gloria opened New Jersey Grocery & Deli with one goal:
				serve every customer like family. More than thirty years later, the same family
				still welcomes neighbors every morning with breakfast, fresh sandwiches,
				groceries, and familiar faces.
			</p>
			<div class="story__milestones">
				<span data-i18n="story.milestoneOne">1994: Oakland Park roots</span>
				<span data-i18n="story.milestoneTwo">2017: Margate location opens</span>
				<span data-i18n="story.milestoneThree">Today: family-owned and hands-on</span>
			</div>
		</div>

		<div class="story__image">
			<img
				src="/images/about-us-new-jersey-deli.avif"
				alt="New Jersey Grocery and Deli family owners"
				loading="lazy"
			/>
			<div class="story__note">
				<strong data-i18n="story.noteTitle">1994 to today</strong>
				<span data-i18n="story.noteCopy">Neighborhood service, family recipes, and a second location opened in 2017.</span>
			</div>
			<img
				class="story__image-accent"
				src="/images/about-us-new-jersey-deli2.avif"
				alt="Family behind New Jersey Grocery and Deli"
				loading="lazy"
			/>
		</div>
	</div>
</section>

<style>
	.story__grid {
		display: grid;
		gap: 2rem;
		align-items: center;
	}

	.story__copy {
		max-width: 660px;
	}

	.story__milestones {
		display: grid;
		gap: 0.65rem;
		margin-top: 1.5rem;
	}

	.story__milestones span {
		display: inline-flex;
		align-items: center;
		min-height: 2.8rem;
		padding: 0.65rem 0.85rem;
		border-left: 4px solid var(--gold);
		border-radius: var(--radius);
		background: var(--white);
		box-shadow: var(--shadow-soft);
		color: var(--blue-deep);
		font-weight: 850;
	}

	.story__image {
		display: grid;
		gap: 1rem;
	}

	.story__image > img:first-child {
		width: 100%;
		aspect-ratio: 4 / 3;
		border-radius: var(--radius);
		background: var(--neutral-strong);
		object-fit: contain;
		object-position: center;
		box-shadow: var(--shadow);
	}

	.story__image-accent {
		width: min(78%, 21rem);
		aspect-ratio: 4 / 3;
		justify-self: end;
		border-radius: var(--radius);
		background: var(--neutral-strong);
		object-fit: contain;
		object-position: center;
		box-shadow: var(--shadow);
	}

	.story__note {
		width: min(calc(100% - 2rem), 22rem);
		justify-self: center;
		padding: 1rem;
		border-left: 5px solid var(--gold);
		border-radius: var(--radius);
		background: var(--white);
		box-shadow: var(--shadow-soft);
	}

	.story__note strong,
	.story__note span {
		display: block;
	}

	.story__note span {
		color: var(--ink-soft);
	}

	@media (min-width: 900px) {
		.story__grid {
			grid-template-columns: minmax(0, 1fr) minmax(360px, 0.78fr);
			gap: 4rem;
		}

		.story__image {
			grid-template-columns: minmax(280px, 340px) auto;
			align-items: center;
			gap: 1.5rem;
		}

		.story__image > img:first-child {
			grid-column: 1 / -1;
		}

		.story__image-accent {
			grid-column: 1 / -1;
			grid-row: 3;
			justify-self: end;
			width: min(82%, 25rem);
			border: 8px solid var(--neutral);
		}

		.story__note {
			grid-column: 1;
			grid-row: 2;
			width: min(100%, 21.25rem);
			max-width: 21.25rem;
			justify-self: end;
		}
	}
</style>
